# Database (uses same MariaDB container as sensea, different database)
DB_HOST=db
DB_NAME=ops_db
DB_USER=snoezelen
DB_PASS=snoezelen_secret
DB_CHARSET=utf8mb4

# JWT
JWT_SECRET=ops-jwt-secret-change-in-production-min-32-chars
JWT_REFRESH_SECRET=ops-refresh-secret-change-in-production

# Mail (for magic link)
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USER=
MAIL_PASS=
MAIL_FROM=noreply-ops@sensea.cc
MAIL_FROM_NAME=sensea OPS

# URLs
APP_URL=http://localhost:8090
FRONTEND_URL=http://localhost:5180

# Sensea API (for revenue data)
SENSEA_API_URL=http://api
SENSEA_API_KEY=ops-dev-api-key-change-in-production

# App
ENV=development
DEBUG=true
APP_TIMEZONE=Europe/Paris
