# Database (uses same MariaDB container as sensae, different database)
DB_HOST=db
DB_NAME=ops_db
DB_USER=snoezelen
DB_PASS=snoezelen_secret
DB_CHARSET=utf8mb4

# JWT
JWT_SECRET=ops-jwt-secret-change-in-production-min-32-chars
JWT_REFRESH_SECRET=ops-refresh-secret-change-in-production

# Mail (for magic link)
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USER=
MAIL_PASS=
MAIL_FROM=noreply-ops@sensae.cc
MAIL_FROM_NAME=sensae OPS

# URLs
APP_URL=http://localhost:8090
FRONTEND_URL=http://localhost:5180

# sensae API (for revenue data)
sensae_API_URL=http://api
sensae_API_KEY=ops-dev-api-key-change-in-production

# App
ENV=development
DEBUG=true
APP_TIMEZONE=Europe/Paris
